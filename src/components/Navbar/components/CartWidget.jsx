import { AiOutlineShoppingCart } from "react-icons/ai";

const CartWidget = () => {
    return (
        <span>
            <AiOutlineShoppingCart /> 0
        </span>
    );
};

export default CartWidget;